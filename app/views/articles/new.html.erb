<div class="container">
  <h2 class="mt-3">Add a Fantasy</h2>

  <%= simple_form_for @article do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :name,
                  required: true,
                  autofocus: true,
                  input_html: { autocomplete: "name" }%>
      <%= f.input :price,
                  required: true,
                  autofocus: true,
                  input_html: { autocomplete: "price" } %>
      <%= f.input :description,
                  required: true,
                  autofocus: true,
                  input_html: { autocomplete: "description" } %>
      <%= f.input :image, as: :file %>
      <%= f.input :service_type, :collection => %w[Travel Experience Exotic Event],
                  required: true,
                  input_html: { autocomplete: "service type" } %>
    </div>

    <div class="form-actions btn btn-outline-dark">
      <%= f.button :submit, "Add Article" %>
    </div>
  <% end %>
</div>
