<div class="navbar navbar-expand-sm navbar-light navbar-lewagon" id="redirect_logged">
  <div class="container-fluid">
    <form class="d-flex my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-light my-2 my-sm-0" type="submit"><i class="fa-solid fa-magnifying-glass"></i></button>
    </form>

    <p> <% if current_user %>
    <%= current_user.email %>
    <% end %> </p>
    
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
          <li class="nav-item dropdown">
            <div class="dropdown-toggle btn btn-outline-light" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">My Profile</div>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <a class="dropdown-item" href="#">My account</a>
              <a class="dropdown-item" href="#">Bookings</a>
              <a class="dropdown-item" href="#">Create an experience</a>

              <div class="logout">
              <%= button_to(
                "Log out",
                destroy_user_session_path,
                data: {turbo: false},
                method: :delete
              ) %>
              </div>
          </li>
          <!--li class="nav-item"-->
          <!--/li-->
      </ul>
    </div>
  </div>
</div>
